[core]
  editor       = vim
  excludesfile = /Users/sam/.gitignore_global
  autocrlf     = false
  safecrlf     = true
[alias]
  a            = add
  br           = branch
  c            = commit
  cm           = commit -m
  co           = "!f() { git checkout \"$@\" && git stash list | grep \"$1:\"; }; f"
  com          = !git checkout master
  cob          = checkout -b
  cz           = !cz commit
  dp           = !git diff -R playground
  dm           = !git diff -R master
  ll           = log --graph --format="%C(yellow)%h%C(red)%d%C(reset) -- %C(bold green)(%ar)%C(reset) %s %C(blue)<%an>%C(reset)"
  po           = "!git push -u origin $(git rev-parse --abbrev-ref HEAD)"
  st           = status
  up           = !git pull --rebase --prune $@ && git submodule update --init --recursive
  bdel         = "!f() { git push --delete origin $1; }; f"
  bclean       = "!f() { git branch --merged ${1-master} | grep -v " ${1-master}$" | xargs git branch -d; }; f"
[push]
  default      = simple
[credential]
	helper       = none
[includeIf "gitdir:/Users/soehlert/projects/work/"]
    [user]
        name  = Sam Oehlert
        email = soehlert@es.net
[includeIf "gitdir:/Users/soehlert/projects/personal/"]
    [user]
        name  = Sam Oehlert
        email = sam.oehlert@gmail.com
# BEGIN ANSIBLE MANAGED BLOCK
[include]
  path = "/Users/soehlert/.gitaliases"
# END ANSIBLE MANAGED BLOCK
